{
  "name": "auxd",
  "version": "0.0.0",
  "private": true,
  "description": "auxd — social album-tracking platform. pnpm + uv monorepo.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "web:dev": "pnpm --filter @auxd/web dev",
    "web:build": "pnpm --filter @auxd/web build",
    "web:lint": "pnpm --filter @auxd/web lint",
    "web:typecheck": "pnpm --filter @auxd/web typecheck",
    "shared:build": "pnpm --filter @auxd/shared-types build"
  }
}
